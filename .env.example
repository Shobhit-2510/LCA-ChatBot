# Copy to `.env` and fill in the key(s) for whichever LLM provider you use.
# .env is git-ignored — never commit real keys.

DEEPSEEK_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DASHSCOPE_API_KEY=        # Qwen
# Ollama runs locally — no key needed.
